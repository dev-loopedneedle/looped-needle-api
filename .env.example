# Looped Needle API - Environment Variables Example
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL server hostname
POSTGRES_SERVER=localhost

# PostgreSQL username
POSTGRES_USER=postgres

# PostgreSQL password
POSTGRES_PASSWORD=your_password_here

# Database name
POSTGRES_DB=looped_needle

# Full database connection URL (optional)
# If not provided, will be auto-constructed from POSTGRES_* variables above
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost/looped_needle

# =============================================================================
# API Configuration
# =============================================================================
# Environment name: local, development, staging, production
ENVIRONMENT=local

# API version prefix for all endpoints
API_V1_PREFIX=/api/v1

# Secret key for application security (required in production)
# Generate a secure random string for production use
SECRET_KEY=your-secret-key-change-in-production

# =============================================================================
# CORS Configuration
# =============================================================================
# Allowed CORS origins
# For development: use * to allow all origins
# For production: specify exact origins as comma-separated list
# Example: CORS_ORIGINS=https://example.com,https://app.example.com
CORS_ORIGINS=*

# =============================================================================
# OpenAI Configuration (Optional)
# =============================================================================
# OpenAI API key for AI features
# Only required if using OpenAI integration endpoints
OPENAI_API_KEY=

# =============================================================================
# Logging Configuration
# =============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log format: json (structured logging) or standard format
LOG_FORMAT=json

# =============================================================================
# Application Settings
# =============================================================================
# Application name displayed in API docs
PROJECT_NAME=Looped Needle API

# Application version
VERSION=1.0.0
